# TeamOps Core Features

This directory is the target home for cross-domain modules:
- auth
- tenant
- user-management
- role-permission
- workflow-config
- settings

During migration, legacy feature folders may still exist at `src/features/*`.
